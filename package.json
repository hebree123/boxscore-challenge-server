{
  "name": "barstool-boxscore-challenge",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-server": "npm i",
    "install-client": "cd boxscore-challenge && npm i",
    "start-server": "node server.js",
    "start-client": "cd boxscore-challenge && npm run start",
    "start": "concurrently \"npm run install-server && npm run start-server\" \"npm run install-client && npm run start-client\""
  },
  "author": "Hebrews Gebremariam",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.5",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongodb": "^5.2.0"
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
